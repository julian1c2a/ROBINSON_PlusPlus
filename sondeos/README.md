# `sondeos/` — experimentos verificados (desde la sesión 2026‑08‑19)

**NO son módulos de producción y NO entran en el build de `lake`** (la `lean_lib` está enraizada en
`ROBINSON_PlusPlus/`; estos ficheros quedan fuera). Se versionan porque **contienen resultados
compilados** que costó obtener y que no deben re‑derivarse.

## Cómo re‑ejecutarlos

```bash
lake env lean sondeos/<fichero>.lean      # desde la raíz de RPP, NUNCA desde FOL/
```

## Qué hay, y qué demostró cada uno

| fichero | sondeo | resultado |
|---|---|---|
| **`CtorDotados.lean`** | **③ paso 1 · los constructores de código dotados + su kit** | ✅ **CERRADO, 443 líneas.** Los seis (`substfcT`/`substtcT`/`substtscT`/`liftcT`/`liftfcT`/`liftscT`) como **definiciones puras** (`funcc (strCode …) …`) — ⚠️ **cero ecuaciones postuladas**: `ax_tc_substfc` es INCONSISTENTE como axioma objeto y no entra por la puerta de atrás. Kit completo (congruencia, `substtc`, `liftc`, invariancia, conmutación META) + `prf_substtc_funcc3`/`prf_liftc_funcc1‑3`, que **no existían**. Puentes `*_termCode` por **`rfl`** — y son **discriminantes**: con el nombre cambiado dentro de `strCode` el `rfl` **falla**, o sea el kernel verifica los seis nombres contra `Minimal/Axioms.lean`. **§4 NUEVO**: las congruencias **INTERNAS** (`pcc_congr_substfcT_arg1/2/3_code`, `pcc_congr_liftcT_arg2_code`), sin las cuales el paso 2 no puede reescribir dentro de `Prov`. **§5**: `fuego_ab`, que encadena la instancia interna de `ax_substfc_forall` con `pcc_dot_un_symm` vía `pcc_eq_trans_code`. Fusión de tres intentos: base `Ctor_desdec` + `prf_congr_funcc3` de `Ctor_directo`; descartada la capa `fn1T/fn2T/fn3T` de `Ctor_generico` (indirección para un conjunto **cerrado** de 6 símbolos) y sus `liftc_inv_fn*T`, **código muerto e indescargable hoy** (piden `∀c, Prf (liftc c X =eq X)` y producción sólo tiene el nivel `zero`). |
| **`Paso2CasoForall.lean`** | **③ paso 2 · el caso `∀`, y DÓNDE se atasca** | 🚩 **El caso `∀` cierra ENTERO, y deja UNA sola hipótesis sin descargar** — que es exactamente `pcc_eval_liftc`: `hLift : Prf (provFromCode (eqc (liftcT (termCode zero) (tcFn s)) (tcFn (liftc zero s))))`. Todo lo demás sale de producción + el kit. ⚠️ **Esto CORRIGE `SustituyendoOpaco.lean`**: aquél refutó la *razón* del gate, no su *conclusión*. Trae además el **enunciado del paso 2** (`evalSubstfcCode` + `pcc_eval_substfc`, con transportes net‑0 y control negativo: con `termCode` el puente es `rfl`, con `tcFn` **falla**), y la decisión de inducción: **fuerte sobre el VALOR del código** con descenso de Cantor —compilado `descenso_un`/`descenso_bin1`/`descenso_bin2`— porque absorbe la recursión **mutua** y porque la inducción sobre el testigo **no vale** (`In` dice que el hijo está en la lista, no que esté **antes**). ⚠️ **La guarda debe ser `isFormCodeE`** (ecuacional, de ①), **no `isFormCodeB`**: de `carc`/`lenc`/`In` no sale la ecuación `X ≐ forallc a` que el axioma necesita, y el paso inductivo **no cerraría**. |
| **`SustituyendoOpaco.lean`** | **③ · ¿hace falta `pcc_eval_liftc` (clausura de `isTermCode` bajo `liftc`)?** | ⛔ **NO. La afirmación del gate es FALSA** — y del mismo modo que ya falló una vez (dijo que faltaba el puente `In` cuando estaba en producción): **infirió una obligación de la presencia SINTÁCTICA de `liftc` sin comprobar si algo lo inspecciona**. Compilado net‑0: `substCodeF v w (∀a) = cons 6̄ (cons (substCodeF (v+1) (liftc 0 w) a) nil)` **por `rfl`** — el sustituyendo sólo se traslada; y **`prf_substfc_arith_open` está en producción con `w : Term` pelado**, cubre los 8 constructores, y aquí se instancia con `w := #0` y con **dos binders anidados** (`liftc 0 (liftc 0 #0)`), sin una sola hipótesis. ⚠️ **Lo que NO dice**: es la versión META (`f : Formula` de Lean); `pcc_eval_substfc` sigue siendo el trabajo grande. ⇒ **③ se reorienta**: no es clausura bajo `liftc`, es que **`substfcT` ni siquiera está definido** (0 ocurrencias en el árbol). ③ empieza definiendo el constructor de código dotado —como `nthcT`/`lencT`, que son `funcc (strCode …) …` y **no llevan axiomas**— y sigue con la ecuación interna por inducción sobre el código, usando `pcc_axiom_inst3` sobre las ocho `ax_substfc_*` y el KIT (`pcc_dot_un`/`pcc_dot_bin`) para el ensamblaje. |
| **`DiscriminaEcuacional.lean`** | **① CERRADO · la discriminación SOBREVIVE a la forma ecuacional** | ✅ **SOBREVIVE, net‑0.** 🔑 **Y no hacen falta 12 lemas de fortalecimiento, ni 3: basta UNO.** `prf_shape_strengthens (X C k n)` transporta por Leibniz desde la forma concreta `C` a la `X` abstracta **sin mirar qué constructor es**, así que los 12 disyuntos son instancias con distinto `C`/`k`/`n` — las tres aridades salen como `prf_shapeNul_str`/`_shapeUn_str`/`_shapeBin_str`. Con eso: `prf_isTermCodeE_str` y `prf_isFormCodeE_str` (los 8 tags) prueban que la forma ecuacional **fortalece** la vieja, y la discriminación se hereda por `impT`: **`crit_isTermCodeE_rejects_implc`** (el junk exacto de `SubCodesCritica`) y **`crit_isFormCodeE_rejects_varc`** (un código de VARIABLE no pasa por `isFormCodeE`), ambos net‑0. ⚠️ **No es fortalecimiento vacuo**: `prf_shapeNul_real`/`_shapeUn_real`/`_shapeBin_real` exhiben códigos reales que lo satisfacen. ⚠️ **`isTermsCodeB` ya estaba en forma ecuacional** — sus disyuntos son `X ≐ nil` y `cOk X …`, y `consOk X` **es** una ecuación: no había nada que reformular. El fichero extiende `DiscriminaTestigoAbierto.lean`, así que ① y ② **componen**. |
| **`DiscriminaTestigoAbierto.lean`** | **② CERRADA · la discriminación SIN clausura sobre el testigo** | ✅ **CERRADA en las DOS caras, net‑0.** Re‑enuncia los dos titulares **sin `hpl`/`hps` sobre `p`**: **término** — `crit_isTCB_junk_refuted_open`, `crit_junk_SubCodesCritica_open` (el junk EXACTO de `SubCodesCritica`) y **`crit_junk_var0_witness`** (testigo **literalmente `#0`**); **fórmula** — `prf_crit_In_F_rejects_open`, `crit_isFCB3_no_termcode_open` y **`crit_isFCB3_no_termcode_var0`**. 🔑 **La maniobra**: NO existe lema de lifting de DERIVACIONES (`PrfH Γ A → PrfH (Γ.map (liftFormula 0)) (liftFormula 0 A)`) — así que el testigo **no va como hipótesis externa sino DENTRO DEL OBJETIVO** del `∃`‑elim, donde el `liftFormula 0` lo aplica uno mismo con `liftF_pairOk`/`liftF_tripleOk` y aparece ya como `pairOk ↑p`. **El lift cae en el sitio que se controla.** Piezas nuevas reutilizables: `liftF_isTermCodeB`/`_isTermsCodeB`/`_isFormCodeB`, `liftF_wfAllT`/`_wfAllTs`/`_pairOk`/`_wfAllF`/`_wfAllTgen`/`_wfAllTsgen`/`_tripleOk`, `liftT_acF`/`_acT`/`_acTs`, `PrfH_inst_wfAllT_open`, `PrfH_inst_wfAllF_open`. ⚠️ `c` conserva su lift‑invariancia, y es legítimo: el `c` del caso de uso es un código construido (`nthc X 2̄`, `implc a b`), no una variable. ⚠️ El fichero **duplica** `ParticionDiscrimina.lean` (los `sondeos/` no se pueden importar entre sí) y añade la sección `CerrarDos` al final. |
| **`TestigoAbierto.lean`** | **② opción (c) · ¿hace falta que el testigo sea CERRADO?** | ✅ **NO — la clausura es un ARTEFACTO DE LA RUTA DE PRUEBA, no del enunciado.** `inst_wfAllT_open (p k)` y sus tres gemelas compilan **net‑0 con `p` y `k` ABSTRACTOS y CERO hipótesis**, incluido **`p := #0`** (el testigo real del `∃`‑elim, que **no** es cerrado — el fichero lo documenta con `liftTerm 0 #0 ≠ #0` por `decide`) y la versión **`PrfH`**, que es donde vive tras el `∃`‑elim. 🔑 **La clave**: `wfAllT` **ya lleva el lift explícito** en su definición; `wfAllT_closed` sólo existe para **quitarlo** y que disparen los `simp`. Si en vez de quitarlo se **lleva**, `FOL.substTerm_liftTerm` hace el trabajo **para cualquier `p`**. ⇒ **(c) es real: no hay que anclar el testigo en ninguna casilla, y `ind`/`listInd` no cambian de aridad.** ⚠️ **Alcance**: mide el **primer** paso del consumo (la instanciación), que es donde se creía que estaba la obstrucción. Los otros dos usos de clausura en `crit_In_rejects_of_pairOk` son **del mismo tipo** (normalizar el lift interno de `boundedIn`; `hAs`/`hBs` para `crit_spec_body`) — misma maniobra repetida, **pero sin compilar**. |
| **`ParticionTresPredicados.lean`** | **la PARTICIÓN en tres predicados mutuamente recursivos** | ✅ **FUNCIONA, 2282 líneas compiladas**, net‑0 (o net‑0 + `prf_axiomsCodeT_eq` donde toca `provFromCode`). `isFormCode` (8 disyuntos) / `isTermCode` (2) / `isTermsCode` (2), **de verdad mutuos**: cada uno consulta la lista testigo del otro. 🔑 **El cuadre es el premio de R‑6**: 8 disyuntos ↔ **8** ecuaciones de `substfc`; 2 ↔ **2** de `substtc`; 2 ↔ **2** de `substtsc`. Hoy la inducción de `pcc_eval_substfc` se atasca **porque 12 ≠ 8**; partida, cada mitad encaja. El total de disyuntos **no crece** (12 = 8+2+2): es partición, no recubrimiento. **A1 y A2 sobreviven al 100 %** y ~527 líneas de A3 son **byte‑idénticas** (verificado por md5). |
| **`ParticionDiscrimina.lean`** | **¿la partición DISCRIMINA, o sólo reparte el defecto?** | ✅ **DISCRIMINA — y no sólo no admite la basura: la REFUTA.** 2704 líneas, `EXIT=0`. `crit_isFC_junk_REFUTED` y `crit_isFCB3_no_termcode` son **net‑0**: el código `implc ⌜x₀⌝ₜ ⌜x₀⌝ₜ` (una «implicación» cuyas dos subfórmulas son códigos de TÉRMINO) que el predicado **fusionado aceptaba**, el partido lo **refuta**. Era el riesgo real de la partición —tres predicados que siguieran aceptando basura no arreglarían nada— y queda cerrado con prueba. Incluye además `Probe/PuenteInNth.lean`: la objeción (D) de `SubCodesCritica` queda **refutada por segunda vez y por identidad de términos** (`boundedIn` **es** literalmente el puente, `rfl`). |
| **`SubCodesWitness.lean`** | **hito (i) · el TESTIGO DE PARSEO de todo código de fórmula** | ✅ **CERRADO, net‑0** (`[propext, choice, Quot.sound]` — ni `prf_axiomsCodeT_eq`, no toca `provFromCode`). `prf_isFCB_subCodes (φ) : Prf (isFCB (objList (subCodes φ)) (formCodeM φ))` y `prf_isFC (φ) : Prf (∃w. isFCB w ⌜φ⌝)`, **sin hipótesis**. `subCodes` salió tal como se propuso, incluido **`subCodesTs [] = [nil]`** (imprescindible: sin él el disyunto 12 de `termsCodeM [t]` se queda sin descargar, porque pide `In (cdrc X) w` con `cdrc X = nil`). 🔑 La inducción **generaliza sobre un superconjunto** (`∀W, subCodes φ ⊆ W → …`) en vez de usar monotonía ⇒ cada nodo se prueba contra la lista COMPLETA y nunca hay que transportar. 3 estrategias independientes, **3 de 3 confirmadas** (compila + fiel + **satisfacible**). ⚠️ Pero **ver `SubCodesCritica.lean`**: cierra la COMPLETITUD, **no** desbloquea el muro. |
| **`SubCodesCritica.lean`** | **hito (i) · el crítico: ¿sirve para lo que hace falta?** | ⛔ **NO, y el defecto es de DISEÑO (mío).** `nodeOk` es **UN SOLO** predicado de 12 disyuntos que **fusiona** las formas de código de FÓRMULA (tags 2‑9) con las de TÉRMINO (tags 0‑1), `nil` y el `cons` genérico ⇒ **`isFC X` no dice que `X` sea código de fórmula**. Compilado net‑0: `prf_isFC_nil`, `prf_isFC_varc` (un código de VARIABLE pasa) y **`prf_isFC_junk`** — `implc ⌜x₀⌝ₜ ⌜x₀⌝ₜ`, una «implicación» cuyas dos subfórmulas son códigos de TÉRMINO, **pasa el reconocedor**. Y `substfc` tiene **8 ecuaciones, una por constructor de FÓRMULA** y **ninguna** para `varc`/`funcc`/`nil`/`cons` ⇒ la inducción interna se atasca. **R‑6 sigue viva palabra por palabra**: hacen falta `isFormCode`/`isTermCode`/`isTermsCode` **separados y mutuamente recursivos**. ✅ Aporta además, net‑0: el **gemelo de términos** (`prf_isTC`/`prf_isTsC`, ~45 líneas), el **transporte** `prf_congr_isFC` (Leibniz bajo el `∃`, `substFormula 0 s A = isFC s` por `rfl`), y `real_mem_subCodes` (**el testigo no tiene basura**: todo miembro es `formCodeM ψ`/`termCodeM t`/`termsCodeM ts`). ⚠️ **Su objeción (D) es FALSA** — el puente `In x w → ∃i<lenc w. nthc w i = x` **sí existe**: `prf_boundedIn_of_In` (`BoundedInPrf.lean:321`), lista abstracta, y A1 ya lo usa. |
| **`A3IsFCBTracked.lean`** | **(A3) · el reflector COMPLETO de `isFCB`** | ✅ **CERRADO.** `pcc_isFCB_tracked (w c) : Prf (isFCB w c ⇒ provFromCode (isFCBDot w c))`, `w` y `c` **ABSTRACTOS**, footprint sancionado. **La jugada**: dotar `In` como su **ÁTOMO** (`inFormCodeFn`) en vez de como su despliegue ∃‑acotado ⇒ el cuerpo del ∀ acotado **no tiene ni un binder**, todo el descenso de `substfc` vive en nivel 0 y `hPsiId` sale por instanciación pura. El problema de los dos niveles De Bruijn **no se resolvió: se disolvió.** `pcc_wfAll_tracked` es UNA línea (`pcc_bdAll_intro wfAll lenc PsiF w` con las 8 obligaciones descargadas). ⚠️ **Dos desviaciones declaradas**: (1) `consOk X` en 11 de los 12 disyuntos —hace falta porque `pcc_eval_carc` sólo evalúa sobre `cons` explícito—, que **restringe** ⇒ no toca solidez, pero **añade una obligación al hito (i)**; (2) la cota sale como `tcFn (lenc w)`, no `lencT (tcFn w)`. |
| **`A3ConsOkRefuta.lean`** | **(A3) · la REFUTACIÓN que mató a 2 de los 4 intentos** | ⛔ **`consOk` GLOBAL es FATAL, y está compilado.** Poner `consOk X` como conjunto **global** (en vez de caso a caso) hace que la teoría objeto **REFUTE** que `nil` sea nodo (`nil_ne_cons`, `CodeDistinct.lean:67`). Y `nil` es **obligatorio**: `termCodeM zero = cons 1 (cons ⌜"0"⌝ (cons nil nil))` tiene `nil` en la casilla 2, y la cola de toda lista de argumentos termina en `termsCodeM [] = nil`. ⇒ `∃w. isFCB w c` se queda **sin testigos** para toda fórmula que mencione un símbolo con argumentos: el teorema sería verdadero **y vacío**. Dos verificadores adversariales lo encontraron **independientemente**. 🔑 **Moraleja**: reforzar el antecedente objeto es la dirección que ABARATA el teorema, y por eso es donde hay que mirar — se detectó porque alguien lo **compiló**, no porque alguien lo leyera. |
| **`InTracked.lean`** | **(A) · el REFLECTOR de la vía (2): A1 y A2** | ✅ **EXISTE, y con footprint sancionado.** Tapa el **único hueco** que tenía la cadena: no había reflector `tracked` de `In` sobre lista **ABSTRACTA** (`pcc_In_lfc_tracked` recurre sobre lista META). **A1** `pcc_boundedIn_tracked`/`pcc_In_tracked` sale por el molde de `pcc_bddCarcDot_reflect`, y **más barato que él**: (a) `pcc_eval_nthc` es **incondicional** salvo `i < lenc p` — que es lo que da el ∃ acotado — mientras que `pcc_eval_carc_nthc` consume `chainOk nil p`, requisito que nuestro testigo (que **no** es cadena de prueba) no cumpliría; (b) sin cruce de frontera numeral, porque el argumento es abstracto. **A2** `pcc_binOk_tracked` mide el caso **MÁXIMO** de `nodeOk` (dos `=eq` + dos `In`) ⇒ subsume los 12: `pcc_eq_tracked` es general y `PrfH_and_intro_code` ensambla. **A3** no tiene hueco de herramienta: `pcc_bdAll_intro` (§40) está completa y su `hbody` es `prf_spec` + MP + A2; y `substfc` atraviesa el `orc` **y el `exc`** — las 8 ecuaciones son teoremas (`ArithPrf.lean:298‑347`). Quedan los **dos niveles De Bruijn** del binder, con precedente (`prf_substfc_exBodyc`). |
| **`ParseWitness.lean`** | **opción (2) · `isFormCode` SIN axiomas, por testigo de parseo — FASE A** | ✅ **LA ARQUITECTURA SALE, Y NET-0.** La vía que julio dejó «ni demostrada ni refutada» está **viva**: `nodeOk`/`wfAll`/`isFCB` definidos con los **12 casos** en vocabulario EXISTENTE, más **`prf_bdAll_numeral`** (`∀i<numeralM n. Φ(i)` desde las `n` instancias — la pieza de viabilidad menos clara, verde a la primera), el puente `objList`⇄meta, `PrfH_congr_nodeOk` (**UNA** Leibniz, no 12) y **`prf_wfAll_objList`** (el ensamblaje), con `⊥` extremo a extremo. **3 decisiones abaratadoras**: pertenencia **sin orden** (`In`, no «antes de `i`») ⇒ componer testigos es gratis y la buena fundamentación la da `prf_cantor_mono`; **las cadenas no llevan predicado** (`substfc` no recurre en `strCodeM`) ⇒ borra un predicado entero y el problema de `Char.ofNat`; **cero monotonía** (probar contra la lista completa vía `prf_In_objList`) ⇒ evita un or-elim de 12 casos por composición. Falta `subCodes` + su inducción meta: **volumen, no diseño**. NO decide la fase B (`pcc_eval_substfc`), común a las dos opciones. |
| **`SegundoMuro.lean`** | **muro de `substfc` · ¿existe de verdad el «segundo muro»?** | ✅ **NO ES UN MURO.** Mitad 1 (**compilada, net‑0**): `hcond_absorbe_extra` — el chasis `pcc_lineWF_tracked_of_schema` **absorbe el conjunto extra**; enmendar los 7 esquemas **no obliga a rehacerlo**. Sale por `Prf₀.c2`/`c3` fuera + `prf_substfc_and` (`substfc` distribuye sobre `andc`) + `PrfH_and_intro_code` dentro. Mitad 2: los 4 ingredientes del reflector **están en producción** — `prf_strong_induction` (⚠️ **existe**: la nota de MEMORY.md era obsoleta), `prf_cantor_mono_left/right`, `pcc_lt_tracked`, y el precedente **`pcc_eval_lenc`** (argumento objeto ABSTRACTO → `Prov`). ⚠️ `pcc_In_lfc_tracked` **no** es precedente: recurre sobre lista **META**. **R‑6 CONFIRMADA** y con especificación exacta: la enmienda pide **DOS** predicados mutuamente recursivos (`isFormCode` 7/7, `isTermCode` **sólo 3/7**), no uno ⇒ el reflector **cuesta el doble** de lo que suponía julio. `isFormCode`/`isTermCode` **no existen** en el árbol. |
| **`DescargaHFN.lean`** | **la comprobación final** | ✅ **EL MÁS IMPORTANTE.** Rehace el piloto con `hFN` **DEMOSTRADA** (vía `Meta/CodeNumeralPrf.lean`). `godelCN_fixedpoint` compila **sin `codeN`, sin `hFN` y sin `tc_cons`**. La vía numeral deja de ser condicional. |
| `PilotoDiagonal.lean` | piloto del lema diagonal (histórico: con `hFN` asumida) | El punto fijo de Gödel **sobrevive** con códigos NUMERALES. `godelCN_fixedpoint` tiene el footprint del original **menos `tc_cons`**. Asume sólo `hFN` (= la salida de S4). Además `provCode_transfer` da la equivalencia con la representación en árbol en **un** paso de Leibniz. |
| **`PilotoRastreada.lean`** | **piloto de la capa RASTREADA** | ⛔ **NEGATIVO, y decide la estrategia.** `prf_cons_eval` permite enfrentar las dos lecturas: `cons 0 nil =eq numeral 2` es demostrable, luego la lectura numeral da `tcFn (cons 0 nil)` con cabeza `⌈σ⌉` y `prf_tc_cons` con cabeza `⌈::⌉`. Son **incompatibles**. La capa rastreada usa `prf_tc_cons` sobre argumentos **ABSTRACTOS** (`pcc_eval_carc (h t)`, `prf_tc_un/bin`), luego **no sobrevive a la reparación**. |
| `S4.lean` | S4 · evaluación de Cantor | Primer eslabón compilado y net‑0: `prf_add_eq_zero_right`. Falta `prf_div2_numeral`. |
| `S3S5.lean` | S3 · simbolismo + S5 · magnitudes | `codeNat` **total y estructural**; 6 teoremas elaboran al instante — Lean **nunca** reduce `codeNat φ`. Y las medidas de S5 (Unicode 19 068 vs índices 45). |
| `PilotoAislado.lean` | fase 2 · familia INVARIANCIA | `substtc_inv_termCode_*` **sin** la lectura sintáctica. Independencia probada por **aislamiento de importaciones**, comprobada por máquina con `#noExiste`. |
| `Magnitud.lean` | S5 · profundidad | `consDepth (formCode ax_tc_succ) = 3873`; log₂(N) de `ax_tc_cons` ≈ 8,66 × 10⁷⁸. |
| `S1Audit.lean` | S1 · auditoría | ⚠️ **Sólo compila en la rama `sondeo/s1-sin-ax-tc-cons`** (necesita `prf_tc_cons`/`tc_cons` como axiomas de Lean). En `master` no. |
| **`RecodCoste.lean`** | **(B) · coste de la recodificación por tabla** | 📏 **Beneficio MAYOR de lo dicho, pero no urgente.** S5 medía un proxy; medido de verdad: **el 98‑99 % del `formCode` de los axiomas del verificador son los nombres de símbolos** (`ax_tc_zero`: 49 015 → ~708 nodos, **69×**). Coste: **~10 teoremas en 4 módulos**, uno de ellos `CodeDecode` (módulo A de NegVerifier, **completo**). ⚠️ Una tabla pura **no es total** (`Term.func` toma String arbitrario) ⇒ hace falta codificación **etiquetada**. **Hoy no es cuello de botella** (build 4‑11 s; `codeNat` nunca se reduce; `axiomsCodeT` ya es opaco a propósito). ⇒ hacerlo **antes** de escribir C‑F de NegVerifier, no ahora. |
| **`CodeNatInj.lean`** | **`NegVerifier` · ¿es `codeNat` inyectiva?** | ✅ **SÍ, y el sustituto de `canon_ne` sale.** `consN_inj` (el emparejamiento de Cantor, `[propext, Quot.sound]`) → `codeNat_inj` → **`codeNat_ne`**, con la base sancionada y **sin axiomas nuevos**. La mitad objeto (`numeral_ne`) **ya existía**. ⇒ el rediseño por numerales es viable. Laterales: **dos `numeral` duplicados** (`Full` y `Meta.Godel`), y `∧` resuelve a `Formula.and`. |
| **`CanonNeRefuta.lean`** | **`NegVerifier` · refutación de `canon_ne`** | ⛔ **NEGATIVO, y decide el frente.** El paso **1.1** de `PLAN-NEGVERIFIER.md` (base de los módulos C y D) es **FALSO**: `cons nil nil = 2 = numeralM 2` son distintos en Lean y **provablemente iguales** en la teoría, así que `canon_ne` daría `⊥`. El plan es de **julio, anterior a la inconsistencia**, y arrastra el **mismo error de categoría**. Salida: pasar a **numerales**, como ADR‑012. |
| `KitPayoff.lean` | (paso 4) · **medición del KIT** | ✅ **NO es un muro.** `pcc_dot_nul`/`_un`/`_bin` salen **por composición de `pcc_dot_cons`, sin inducción nueva** (nul: sólo código; un: 1 paso interno; bin: 2 anidados). Hizo falta `pcc_dot_cons_symm`, vía `pcc_eq_symm_code_internal` — que volvió con `BdAllIntroPrf` en el paso 3. ⚠️ **El coste real no está en el KIT sino en `CodeTreeReflect`**: `prf_tc_objAt` es recursión sobre `CTree` que hay que mover entera dentro de `Prov`. Todas las piezas existen (`pcc_eq_trans_code` + las congruencias internas del KIT, que **sobreviven**). |
| `TcFormPayoff.lean` | (paso 3) · **medición de `prf_tc_form`** | ✅ **NO es un muro.** El sustituto directo es una línea y net‑0, pero el lado derecho (`termCode (formCode φ)`) **no es negociable**: `inDot` está fijado por `D3DottedPrf`, activo y net‑0, y ese código **es el objetivo de D3**. La salida: **D1 dota `prf_formCode_numeral`** y da el puente entre las dos formas DENTRO de `Prov` ⇒ `pcc_to_formCode`/`_imp`. **Estrategia: no tocar definiciones, convertir en la frontera.** |
| `CarcPayoff.lean` | (a.2) · **el rédito de `pcc_dot_cons`** | ✅ **`pcc_eval_carc` RECUPERADO**, mismo enunciado y mismo footprint. El viejo (`cuarentena/EvalListPrf.lean:123`) cerraba con `prf_tc_cons'` — el puente que murió con la reparación; la sustitución es **un único `pcc_rw` con `pcc_dot_cons`**, y los pasos 1‑3 quedan intactos. Confirma que el keystone `EvalListPrf` es repatriable. |

## ⚠️ Dos trampas metodológicas aprendidas hoy (no repetir)

1. **La alcanzabilidad por `import` da FALSOS NEGATIVOS.** Concluí que Gödel I estaba limpio porque
   `DiagonalTwo` no alcanza `TcArithPrf`; se me escapó `Meta/Diagonal.lean`, que tiene el mismo
   puente en la capa ω. **Sólo `#print axioms` es concluyente.**
2. **Un crawler de dependencias transitivas tampoco sirve**: Lean 4.31 devuelve `value? = NONE`
   para teoremas importados, así que sólo recorre **tipos**, no pruebas. Medido.
   **La técnica que SÍ funciona** es la de `S1Audit`: convertir el puente sospechoso en **`axiom` de
   Lean** y dejar que `#print axioms` delate a sus consumidores.
